# Copy this file to .env and fill in your own proxy URL.
# API keys are NOT set here anymore — they live server-side in the
# calmcare-proxy Vercel project's environment variables, never in the
# frontend. See /calmcare-proxy/README.md for deploy instructions.

VITE_PROXY_URL=https://your-proxy-project.vercel.app/api/chat
